#pragma once

// adjust all values (current values [3, 20] only account for the fact that we are temporarily using a 9x9 grid)
constexpr int GRID_SIZE = 3;
constexpr int TILE_SIZE = 20; // x32 pixels
